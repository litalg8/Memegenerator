// "onClickEl(event)" onmousemove="onMoveEl(event)" onmouseup="onPlaceEl(event)

// function canvasClicked(ev) {
//     ev.preventDefault();
//     var offsetX = ev.offsetX;
// console.log('firstDraw: offsetX', offsetX)
// var offsetY = ev.offsetY;
// console.log('firstDraw: offsetY', offsetY)
// }

// function onAddText() {
//     var elTxtInput = document.querySelector('line-edit');
//     if (!elTxtInput.value) return;
//     elTxtInput.value = '';
//     addLine();
// }

// gCanvas.addEventListener("mousedown", onDragEl);
// gCanvas.addEventListener("mousemove", onMoveEl);
// gCanvas.addEventListener("mouseup", onPlaceEl);
// gCanvas.addEventListener("touchstart", onDragEl);
// gCanvas.addEventListener("touchmove", onMoveEl);
// gCanvas.addEventListener("touchend", onPlaceEl);